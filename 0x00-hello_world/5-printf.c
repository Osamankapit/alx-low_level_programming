#include <stdio.h>
/**
* main -Entry point
*
* Description: Prints "with proper gr* ammar, but the outcome is a peice * of art,"
*
* Return: Always 0 (success)
*/
int main(void)
{
printf("with proper grammar, but theoutcome is a piece of art,\n");
return (0);
}
